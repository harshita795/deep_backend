# Python environment tracking
.venv/
__pycache__/
*.pyc

# Project lock file configuration (Optional to ignore, but standard if you prefer clean repos)
.python-version
game_state.jsonl
game_events.jsonl
